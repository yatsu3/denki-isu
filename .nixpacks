[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install", "npm run server:install"]

[phases.build]
cmds = ["echo 'Project build completed successfully'"]

[start]
cmd = "npm start" 