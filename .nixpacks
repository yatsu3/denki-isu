[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
cmds = ["echo 'Project build completed successfully'"]

[start]
cmd = "cd server && npm start" 